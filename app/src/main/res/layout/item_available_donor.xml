<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="horizontal"
    android:paddingVertical="12dp"
    android:paddingHorizontal="16dp"
    android:background="?attr/selectableItemBackground"
    android:clickable="true"
    android:focusable="true"
    android:gravity="center_vertical">

    <!-- Blood Group Badge (rounded square) -->
    <TextView
        android:id="@+id/tvDonorBloodBadge"
        android:layout_width="44dp"
        android:layout_height="44dp"
        android:text="B+"
        android:textColor="#FFFFFF"
        android:textStyle="bold"
        android:textSize="14sp"
        android:gravity="center"
        android:background="@drawable/bg_blood_group"
        android:layout_marginEnd="14dp" />

    <!-- Name + Info -->
    <LinearLayout
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:orientation="vertical"
        android:layout_gravity="center_vertical">

        <TextView
            android:id="@+id/tvDonorName"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Rakibul Islam"
            android:textColor="#1A1A1A"
            android:textSize="15sp"
            android:fontFamily="sans-serif-medium"
            android:textStyle="bold" />

        <TextView
            android:id="@+id/tvDonorPhone"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Last donated: 3 mos ago"
            android:textColor="#999999"
            android:textSize="12sp"
            android:layout_marginTop="2dp" />
    </LinearLayout>

    <!-- Call Button -->
    <ImageView
        android:id="@+id/ivCallDonor"
        android:layout_width="36dp"
        android:layout_height="36dp"
        android:layout_gravity="center_vertical"
        android:src="@drawable/ic_phone_bold"
        app:tint="#C0182A"
        android:padding="6dp"
        android:background="?attr/selectableItemBackgroundBorderless" />

</LinearLayout>
